import { Container, Overlay } from "./styles";

export const Modal = ((): JSX.Element => {
  return (
    <>
      <Overlay show={true}>
        <Container>
          
        </Container>
      </Overlay>
    </>
  );
});